'use strict';

/**
 * platforms-connect service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::platforms-connect.platforms-connect');

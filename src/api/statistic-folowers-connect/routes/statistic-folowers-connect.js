'use strict';

/**
 * statistic-folowers-connect router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::statistic-folowers-connect.statistic-folowers-connect');
